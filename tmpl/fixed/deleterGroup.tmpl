{{.AUTO_GENERATED_ANNOTATION}}

package {{.BASIC_PACKAGE_NAME}}

import (
	{{.BASE_PACKAGE_IMPORT}}
	{{.DELETER_PACKAGE_IMPORT}}
)

type deleterGroup struct {
	client {{.BASE_PACKAGE_NAME}}.IClient
}

{{ range .tables }}
// {{.name}}
func (receiver deleterGroup) {{.name}}() *{{$.DELETER_PACKAGE_NAME}}.{{.deleterWrapperStructName}} {
    return (&{{$.DELETER_PACKAGE_NAME}}.{{.deleterWrapperStructName}}{}).Init(receiver.client)
}
{{- end }}