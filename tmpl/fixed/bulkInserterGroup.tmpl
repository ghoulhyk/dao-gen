{{.AUTO_GENERATED_ANNOTATION}}

package {{.BASIC_PACKAGE_NAME}}

import (
	{{.BASE_PACKAGE_IMPORT}}
	{{.INSERTER_PACKAGE_IMPORT}}
)

type bulkInserterGroup struct {
	client {{.BASE_PACKAGE_NAME}}.IClient
}

{{ range .tables }}
// {{.name}}
func (receiver bulkInserterGroup) {{.name}}() *{{$.INSERTER_PACKAGE_NAME}}.{{.bulkInserterWrapperStructName}} {
    return (&{{$.INSERTER_PACKAGE_NAME}}.{{.bulkInserterWrapperStructName}}{}).Init(receiver.client)
}
{{ end }}