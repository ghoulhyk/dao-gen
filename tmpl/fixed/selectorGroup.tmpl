{{.AUTO_GENERATED_ANNOTATION}}

package {{.BASIC_PACKAGE_NAME}}

import (
	{{.BASE_PACKAGE_IMPORT}}
	{{.SELECTOR_PACKAGE_IMPORT}}
)

type selectorGroup struct {
	client {{.BASE_PACKAGE_NAME}}.IClient
}

{{ range .tables }}
// {{.name}}
func (receiver selectorGroup) {{.name}}() *{{$.SELECTOR_PACKAGE_NAME}}.{{.selectorWrapperStructName}} {
    return (&{{$.SELECTOR_PACKAGE_NAME}}.{{.selectorWrapperStructName}}{}).Init(receiver.client)
}
{{- end }}